# Architecture

```text
Next.js Frontend
      |
      | REST API
      v
Spring Boot Backend
      |
      | JPA
      v
PostgreSQL
```

Phase 1 keeps public, admin, news, forum, report, and notification boundaries separate.
